server:
  port: 8088

spring:
  datasource:
    url:  jdbc:postgresql://localhost:5432/idioma
    username: postgres
    password: geek
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    generate-ddl: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect